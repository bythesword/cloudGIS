20231227
    1、包网球与DrawCommand的问题
        pickup需要包围球
        A、在内涝大范围的平面下，使用包网球，产生问题
            a、包围球的直径影响在近距离的可视情况，即包围球范围内可见，范围外，就消失
            b、高空视角不影响
        B、补偿锯齿算法，产生了版本差异，需要使用家里电脑的版本进行复原
            （1）、RGBA.png的被替换了
            （2）、显示的补偿效果与家里（word）的不一致，待查
    2、git的版本或文件的恢复使用熟练度问题



20231121
    1、风场”全部风场数据模式，鸟瞰没问题，局部看，例子过大
        A、分析：纹理模式FBO，被放大了
        B、线路规划1：
                (1)、只处理局部风场，鸟瞰=也是局部（全部的局部）。
                (2)、每次鼠标事件，重新初始化
                
                (3)、需要得到网格的大小，FBO的构成，uniform是变化的
                (4)、屏幕空间与网格的对应关系，大一圈，
                    两点（经纬度）的米数
                    屏幕空间对应的矩形，坐下，右上。越界则是全部,
                    层级判断，
                            按照网格数量百分
                            按照图层层级判断
                (5)、粒子数量与网格数量的关系，
                    如果使用全局粒子数量，可能会出现局部粒子数量超标问题，即：网格上全部都是粒子
                (6)、以全球风场进行试验，
                    全球风场需要网格化，目前是一个大网格
        C、解决：

    2、webGL的可以绑定的纹理数量
    3、png的float模式


20231117
    1、使用DEM画mesh后会有接缝漏空问题
        不使用高程即可，
        使用高程，就会出现
    2、使用透明，会全部透明，
        A、透明部分消失，ok
            discard+手工透明设置
        B、不透明部分变成了叠加透明，ok
            与DrawCommand的pass相关，增加一个alpha 人工决定透明问题
        C、水波折射的会增加不同颜色的折射，ok
            discad后，解决
        D、pass: fbo == undefined ? Cesium.Pass.TRANSLUCENT : Cesium.Pass.OPAQUE,
            解决通过discrd，而不是return alpha 解决（估计通过renderStat的blending 也可以，未实验）